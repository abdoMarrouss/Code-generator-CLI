import { Module } from '@nestjs/common';
import { {{name}}Service } from "./services/user.service";
import { {{name}}Controller} from "./controllers/user.controller";
import {{name}}, {{name}}Schema } from "./schemas/user.schema";
import { MongooseModule} from "@nestjs/mongoose";




@Module({
  imports: [
    MongooseModule.forFeature([
      {
        name: {{name}}.name,
        schema: {{name}}Schema
      },
    ])
  ],
  controllers: [{{name}}Controller],
  providers: [{{name}}Service]
})
export class {{name}}Module {}

