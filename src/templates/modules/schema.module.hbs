import { Module } from '@nestjs/common';
import { {{name}}Service } from "./services/{{labels.code}}.service";
import { {{name}}Controller} from "./controllers/{{labels.code}}.controller";
import { {{name}}, {{name}}Schema } from "./schemas/{{labels.code}}.schema";
import { MongooseModule} from "@nestjs/mongoose";




@Module({
  imports: [
    MongooseModule.forFeature([
      {
        name: {{name}}.name,
        schema: {{name}}Schema
      },
    ])
  ],
  controllers: [{{name}}Controller],
  providers: [{{name}}Service]
})
export class {{name}}Module {}

