import { Controller, Get, Post, Body, Param, Delete, Put, Query, Req } from "@nestjs/common";
import { {{name}}Service } from "../services/{{labels.code}}.service";
import { Create{{name}}Dto } from "../dto/create-{{labels.code}}.dto";
import { Update{{name}}Dto } from "../dto/update-{{labels.code}}.dto";
import { {{name}} } from "../schemas/{{labels.code}}.schema";
import { Request } from "express";


@Controller("{{plural}}")
export class {{name}}Controller {
  constructor(private readonly {{labels.code}}Service: {{name}}Service) {
  }

  @Post()
  create(@Body() create{{name}}Dto: Create{{name}}Dto) {
    return this.{{labels.code}}Service.create(create{{name}}Dto);
  }

 @Get()
  findAll() {
    return this.{{labels.code}}Service.findAll();
  }



  @Get(":id")
  findOne(@Param("id") id: string) {
    return this.{{labels.code}}Service.findOne(id);
  }

  @Put(":id")
  update(@Param("id") id: string, @Body() update{{name}}Dto: Update{{name}}Dto) {
    return this.{{labels.code}}Service.update(id, update{{name}}Dto);
  }

  @Delete(":id")
  remove(@Param("id") id: string) {
    return this.{{labels.code}}Service.remove(id);
  }


}












